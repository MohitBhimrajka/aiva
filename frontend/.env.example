# .env.example - Copy this to .env and fill in your secrets

# === POSTGRESQL SERVICE ===
# Credentials used by the PostgreSQL container to initialize itself.
# The backend service will also use these to connect.
POSTGRES_USER=supervity_user
POSTGRES_PASSWORD=supervity_password
POSTGRES_DB=supervity_ap_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# === BACKEND JWT AUTHENTICATION ===
# Generate a new secret key for your project.
# You can generate one with: openssl rand -hex 32
JWT_SECRET_KEY=your_super_secret_random_key_here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# === AI SERVICES ===
# Get your API key from Google AI Studio
GEMINI_API_KEY=your_gemini_api_key_here

# === FRONTEND (For reference, usually configured in docker-compose) ===
# FRONTEND_URL=http://localhost:3000
# NEXT_PUBLIC_API_URL=http://localhost:8000
